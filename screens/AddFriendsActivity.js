import React from "react"
import {Text, View, StyleSheet,TouchableOpacity} from "react-native"

export default class AddFriendsActivity extends React.Component{
    render(){
        return(
            <Text>Add friends to activity</Text>
        )
    }
}